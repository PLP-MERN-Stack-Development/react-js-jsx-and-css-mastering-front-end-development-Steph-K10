{
  "version": 2,
  "outputDirectory": ".",
  "installCommand": "echo 'No installation needed for CDN setup'",
  "buildCommand": "echo 'No build needed for CDN setup'",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}